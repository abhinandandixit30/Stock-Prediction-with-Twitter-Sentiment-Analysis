# -*- coding: utf-8 -*-
"""Stock Prediction With Twitter Sentiment .ipynb

Automatically generated by Colaboratory.

Original file is located at
    https://colab.research.google.com/drive/1jISZZUfdQjSXeFQ_pvdxb_aWl42PgFxY
"""

import numpy as np
import pandas as pd

df=pd.read_csv('data.csv',encoding='ISO-8859-1')

df

from sklearn.ensemble import RandomForestClassifier

from sklearn.naive_bayes import GaussianNB
clf=GaussianNB()

y=df['Polarity']

y

x=df['Tweet']
num_word=5000
from keras.preprocessing.text import Tokenizer
tokenizer = Tokenizer(num_words=num_word)
tokenizer.fit_on_texts(x)
x_token = tokenizer.texts_to_sequences(x)

train=pd.DataFrame(x_token)

train

train.replace(np.nan,0,inplace=True)

train

from sklearn.model_selection import train_test_split
from sklearn.metrics import accuracy_score, classification_report

x_train,x_test,y_train,y_test=train_test_split(train,y,test_size=0.3,random_state=0)

x_train.shape

forest=RandomForestClassifier(criterion='entropy',n_estimators=10,random_state=1,n_jobs=2)
forest.fit(x_train,y_train)
forest.score(x_train,y_train)

y_pred=forest.predict(x_test)

from sklearn.metrics import accuracy_score

print('Accuarcy:%.2f'%accuracy_score(y_test,y_pred))

from matplotlib import pyplot as plt

plt.hist(y)

plt.hist(y_pred)
plt.show()





